# 人机协同工作规范

版本：1.0
状态：已采用
定位：本项目长期协作、知识沉淀、质量验证和安全约束的默认规范。

## 1. 使用原则

1. 先理解现状，再修改文件；项目局部规则和当前用户要求优先于本规范。
2. 只记录已经确认、可复用的规则和事实；不将推断、聊天记录或占位内容伪装成结论。
3. 修改应小步、可检查、可回滚，避免无关重构、格式噪音和重复文档。
4. 每项工作以可验证完成为目标；实际运行命令及结果优先于主观判断。
5. 删除重要内容、发布、外部写入或传输敏感信息前必须取得明确授权。

## 2. 标准工作流程

### 理解

- 阅读 `AGENTS.md`、项目入口和与任务有关的项目知识文件。
- 区分已确认事实、合理推断和待确认事项，识别文件职责和验收标准。

### 方案与实施

- 采用符合现有目录职责的最小可行改动；重大取舍或高风险变更先请求确认。
- 不覆盖用户未提交修改；复杂逻辑只添加必要的解释性说明。

### 验证与交付

- 先运行最相关的检查，再根据风险扩大到构建、测试或集成验证。
- 交付说明修改范围、关键假设、实际验证、未解决问题和残余风险。

## 3. 目录与文件命名

- 根目录只保留项目入口、智能体规则、协同规范和当前交接材料；稳定知识进入 `项目知识/`。
- 新增稳定目录优先使用小写英文和短横线，并按职责命名；已有中文文件名不要求批量改名。
- 新增验证脚本放在 `scripts/`，使用可识别动作前缀，例如 `verify-*`、`build-*` 或 `diagnose-*`。
- 一项事实只能有一个当前权威文档；移动或归档文档时同步更新索引和引用。

## 4. 自主权限与成本策略

- L0：只分析和解释。
- L1：调查并提出方案，确认后修改。
- L2：直接修改文件并运行本地验证。
- L3：执行完整本地工作流；外部副作用仍需用户授权。

默认采用 L2。默认成本策略为“经济优先、质量兜底”：C0 工具优先，用于搜索、格式化、检查和构建；C1 轻量模型，用于摘要、索引和明确规则下的小修改；C2 标准模型，用于常规开发、修复和测试；C3 高能力模型，用于高风险、跨模块架构和复杂故障。

### 需要用户决策的运行配置

项目可在 `项目知识/执行配置.md` 中记录模型映射、预算、升级阈值和子智能体授权。运行环境未提供相应路由时，不假定能够切换模型或自动创建子智能体；应使用当前能力和本地工具完成可验证工作。

## 5. 项目知识与跨对话恢复

- 跨对话以项目文件为事实来源，不假定可取得完整聊天历史。
- 新对话先读取 `AGENTS.md`、`项目知识/索引.md`、`项目知识/当前状态.md` 和 `项目知识/任务恢复卡.md`，再按索引路由读取最多两个相关来源。
- 需求、故障、决策、状态变化或任务结束时更新受影响知识文件；没有变化时不重写。
- 长期且跨项目可复用的规则留在本文件，项目细节留在 `项目知识/`。

## 6. 质量与安全底线

- 不泄露或提交令牌、密钥、代理凭据、个人敏感信息和不必要的第三方数据。
- 不通过删除、绕过或弱化检查来制造“通过”的结果。
- 规范、项目事实和用户要求冲突时，明确说明冲突、影响和待确认项。
- 规范与项目知识结构变更后运行 `node scripts/verify-collab-spec.mjs`。
