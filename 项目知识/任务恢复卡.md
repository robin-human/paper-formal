# 任务恢复卡

最后更新：2026-08-08
当前任务：落实软件开发协作规范到 LaTeX 论文写作规范项目
当前分支/版本：`main`，已跟踪 `origin/main`

## 一句话背景

项目维护一份中文 LaTeX 论文写作规范，现已接入项目知识体系和 GitHub 代理排障规则。

## 已确认事实

- GitHub 远程为 `https://github.com/robin-human/paper-formal.git`。
- 本仓库 Git 代理使用 `http://127.0.0.1:7897`；该端口来自 Clash Verge 的当前实际监听状态。
- 原 `7890` 端口未监听，不能作为 Git 代理端口。

## 最近一次进展

从 `robin-human/ai-software-development-specification` 的 `main` 分支下载协作规范，并为本项目创建适用的入口规则、项目知识文件和校验脚本。

## 下一步动作

1. 运行 `node scripts/verify-collab-spec.mjs`。
2. 提交变更后运行 `git push`。
3. 后续任务先从 `AGENTS.md` 和 `项目知识/索引.md` 恢复上下文。

## 不要重复做的事

- 不要把浏览器可访问 GitHub 误判为 Git 命令行已配置代理。
- 不要猜测 Clash Verge 端口；先用监听信息验证。
- 不要将代理凭据或访问令牌写入仓库。
